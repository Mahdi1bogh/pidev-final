{% extends 'base-back.html.twig' %}

{% block title %}category{% endblock %}

{% block body %}
    <h1>category</h1>

    <table class="table" style="margin-left:500px; margin-top=200px">
        <tbody>
            <tr>
                <th>category</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom_category</th>
                <td>{{ category.name }}</td>
            </tr>
        
        </tbody>
    </table>

    <a href="{{ path('app_category_index') }}">retour à la liste</a>

    <a href="{{ path('app_category_edit', {'category': category.id}) }}">edit</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
