<form method="post" action="{{ path('app_tournois_delete', {'id': tournoi.id}) }}" onsubmit="return confirm('Vous-etes sure d'eefacer se champs?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournoi.id) }}">
    <button class="btn">Delete</button>
</form>
